---
version: '3.8'
services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        OS_VERSION: 12
        VERSION: 0.0.1
      labels:
        APPLICATION: POS_Server
        # other labels to add later
    image: kevharv/pos-server:latest
    container_name: POS_Server
    depends_on:
      - database
    env_file:
      - .env.development
      - .env.local
    expose:
      - 80
      - 443
    ports:
      - 80:80
      - 443:443
    restart: no
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  database:
    image: postgres:16
    container_name: POS_DB
    volumes:
      - db_data:/var/lib/postgresql/data
    env_file:
      - .env.db
    ports:
      - 5432:5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready", "-d", "db_prod"]
      interval: 30s
      timeout: 30s
      retries: 5
      start_period: 10s  

volumes:
  db_data:
    name: Database_Volume